# Terraform Workspaces

A workspace is an isolated environment with its own state file.

Use cases:
- dev / staging / prod environments
- Environment isolation using same configuration

Each workspace maintains a separate state automatically.
